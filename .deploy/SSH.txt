notepad $env:USERPROFILE\.ssh\config

# COLE ISSO DENTRO
Host joycebordados
    HostName bordado.fariasfardas.com
    User micaelfarias.dev

git remote | xargs -I {} git push {} --all &&
ssh joycebordados 'cd ~/app_repo; git pull origin main; sudo systemctl restart bordado-farias; exit'